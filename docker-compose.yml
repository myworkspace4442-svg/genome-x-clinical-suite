version: '3.8'

services:
  genomex_app:
    build: .
    container_name: genomex_suite_container
    ports:
      - "8501:8501"
    volumes:
      - ./data_cache:/app/data_cache
    environment:
      - NCBI_API_KEY=your_optional_api_key
      - PYTHONPATH=/app
    restart: always
